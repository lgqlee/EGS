var controller_parse = require('./lib/controller_parse').parse;
var apps = require('./lib/core_loader').apps;

module.exports.start = function (callback) {
    apps.forEach(function (app) {
        
    });
};